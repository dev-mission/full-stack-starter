<%- contentFor('head') %>
<base href="/admin/">
<script type="text/javascript">
window.env = {};
window.env.ASSET_HOST = '<%= process.env.ASSET_HOST %>';
</script>
<%- contentFor('body') %>
<app-admin>Loading...</app-admin>
<% for (let entry of ['admin']) { %>
  <% for (let chunk of webpackStats.chunks[entry]) { %>
    <script type="application/javascript" src="<%= chunk.publicPath %>"></script>
  <% } %>
<% } %>
